<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>KARAT — Тренеры</title>

  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f6f6f6;
      margin: 0;
      padding: 20px;
      text-align: center;
    }

    .back-btn {
      position: absolute;
      left: 15px;
      top: 15px;
      background: none;
      border: none;
      font-size: 18px;
      cursor: pointer;
    }

    h1 {
      margin-top: 40px;
      margin-bottom: 25px;
      font-size: 28px;
    }

    .trainer-card {
      background: #fff;
      border: 2px solid #333;
      border-radius: 12px;
      padding: 15px;
      margin: 10px auto;
      max-width: 400px;
      text-align: left;
    }

    .trainer-name {
      font-size: 20px;
      font-weight: bold;
      margin-bottom: 5px;
    }

    .trainer-city {
      font-size: 16px;
      color: #555;
      margin-bottom: 10px;
    }

    .actions {
      display: flex;
      gap: 10px;
    }

    .btn-small {
      flex: 1;
      padding: 10px;
      background: #fff;
      border-radius: 10px;
      border: 2px solid #333;
      font-size: 16px;
      font-weight: bold;
      cursor: pointer;
      text-decoration: none;
      color: #000;
      text-align: center;
    }

    .btn-small:hover {
      background: #eaeaea;
    }
  </style>
</head>

<body>

<button class="back-btn" onclick="location.href='admin_menu.html'">⬅️ В главное меню</button>

<h1>Тренеры</h1>

<div id="list">Загрузка списка тренеров…</div>

<script>
// Твой API
const API_URL = "https://script.google.com/macros/s/AKfycbydYQAoOMHlIAEZIsSUu3sNALYsltItXaBrc6qYHkUdmRvbfgIAutkhgV1Yowpw46WmFg/exec";

// Загружаем список тренеров
fetch(API_URL + "?trainersList=1")
  .then(r => r.json())
  .then(data => {
    if (!data.ok || !data.trainers) {
      document.getElementById("list").innerText = "Ошибка загрузки данных.";
      return;
    }

    const arr = data.trainers;
    const container = document.getElementById("list");
    container.innerHTML = "";

    arr.forEach(t => {
      const card = document.createElement("div");
      card.className = "trainer-card";

      card.innerHTML = `
        <div class="trainer-name">${t.name}</div>
        <div class="trainer-city">Город: ${t.city}</div>

        <div class="actions">
          <a class="btn-small" href="tel:${t.phone || ''}">📞 Позвонить</a>
          <a class="btn-small" href="https://t.me/${(t.telegram || '').replace('@','')}" target="_blank">✉️ Telegram</a>
        </div>
      `;

      container.appendChild(card);
    });
  })
  .catch(err => {
    console.error(err);
    document.getElementById("list").innerText = "Ошибка загрузки списка.";
  });
</script>

</body>
</html>
